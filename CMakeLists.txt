cmake_minimum_required(VERSION 3.16)
project(repo_binaries CXX)
set(CMAKE_CXX_STANDARD 17)
add_executable(binary-demo src/main.cpp)
target_compile_definitions(binary-demo PRIVATE PLATFORM_NAME="${PLATFORM_NAME}")
